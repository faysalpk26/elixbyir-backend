const express = require('express');
const router = express.Router();
const User = require('../models/userModel');

// User routes are primarily in authRoutes.js
// Add any additional user-specific routes here

module.exports = router;